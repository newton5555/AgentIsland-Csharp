<UserControl x:Class="AgentIsland.UI.NotesSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:Background="Black"
             xmlns:ui="clr-namespace:AgentIsland.UI">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SettingsStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Margin="14,18,14,6">
        <ui:SettingsRowControl Title="What's new in this version">
            <ui:PillButtonControl x:Name="ViewButton" />
        </ui:SettingsRowControl>

        <ui:SettingsRowControl Title="Product guide">
            <ui:PillButtonControl x:Name="GuideButton" />
        </ui:SettingsRowControl>

        <ui:SettingsRowControl Title="Full changelog on the website">
            <ui:PillButtonControl x:Name="ChangelogButton" />
        </ui:SettingsRowControl>
    </StackPanel>
</UserControl>
