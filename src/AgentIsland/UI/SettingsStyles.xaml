<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:AgentIsland.UI"
                    xmlns:charts="clr-namespace:AgentIsland.UI.Charts">

    <!-- Dark ComboBox Item Template -->
    <ControlTemplate x:Key="DarkComboBoxItemTemplate" TargetType="{x:Type ComboBoxItem}">
        <Border x:Name="Row" CornerRadius="5" Padding="8,5,8,5" Background="Transparent">
            <ContentPresenter TextBlock.Foreground="#DEFFFFFF" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Row" Property="Background" Value="#1FFFFFFF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Row" Property="Background" Value="#26FFFFFF" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Dark ComboBox Item Style -->
    <Style x:Key="DarkComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Template" Value="{StaticResource DarkComboBoxItemTemplate}" />
        <Setter Property="FontFamily" Value="{x:Static charts:IslandFonts.Ui}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <!-- Dark ComboBox Template -->
    <ControlTemplate x:Key="DarkComboBoxTemplate" TargetType="{x:Type ComboBox}">
        <Grid>
            <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="Face" CornerRadius="7" Background="#0DFFFFFF"
                                BorderBrush="#1AFFFFFF" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" Margin="10,0,4,0"
                                                  HorizontalAlignment="Left" VerticalAlignment="Center" />
                                <Path Grid.Column="1" Margin="0,0,9,0" VerticalAlignment="Center"
                                      Data="M 0 0 L 3.5 3.5 L 7 0" Stroke="#8CFFFFFF" StrokeThickness="1.4" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Face" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              Margin="10,0,24,0" HorizontalAlignment="Left"
                              VerticalAlignment="Center" IsHitTestVisible="False"
                              TextBlock.Foreground="#F2FFFFFF" />
            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                   VerticalOffset="4" AllowsTransparency="True" Focusable="False"
                   PopupAnimation="Fade">
                <Border Background="#FF15171C" CornerRadius="8" BorderBrush="#21FFFFFF"
                        BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}" Padding="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsPresenter />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
    </ControlTemplate>

    <!-- Dark ComboBox Style -->
    <Style x:Key="DarkComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="Template" Value="{StaticResource DarkComboBoxTemplate}" />
        <Setter Property="Foreground" Value="#F2FFFFFF" />
        <Setter Property="FontFamily" Value="{x:Static charts:IslandFonts.Ui}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Height" Value="28" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItemStyle}" />
    </Style>

    <!-- Settings Section Header Style -->
    <Style x:Key="SettingsSectionHeaderStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{x:Static charts:IslandFonts.Ui}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="#57FFFFFF" />
        <Setter Property="Margin" Value="10,14,10,6" />
    </Style>

    <!-- SettingsRowControl Template -->
    <ControlTemplate x:Key="SettingsRowControlTemplate" TargetType="{x:Type ui:SettingsRowControl}">
        <Border x:Name="RootBorder"
                CornerRadius="8"
                Padding="10,11,10,11"
                Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Left: Title, dot, chip, subtitle -->
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Dot -->
                        <Ellipse x:Name="PART_DotEllipse"
                                 Width="7" Height="7"
                                 VerticalAlignment="Center"
                                 Margin="0,0,8,0"
                                 Visibility="Collapsed" />
                        <!-- Title -->
                        <TextBlock x:Name="PART_TitleBlock"
                                   VerticalAlignment="Center"
                                   FontWeight="Medium"
                                   Foreground="#EBFFFFFF"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="300" />
                        <!-- Chip -->
                        <Border x:Name="PART_ChipBorder"
                                CornerRadius="3"
                                Background="#0FFFFFFF"
                                Padding="5,2,5,2"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <TextBlock x:Name="PART_ChipBlock"
                                       FontFamily="{x:Static charts:IslandFonts.Ui}"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="#99FFFFFF" />
                        </Border>
                    </StackPanel>

                    <!-- Subtitle -->
                    <TextBlock x:Name="PART_SubtitleBlock"
                               FontFamily="{x:Static charts:IslandFonts.Ui}"
                               FontSize="11"
                               Foreground="#8CFFFFFF"
                               Margin="0,2,0,0"
                               TextWrapping="Wrap"
                               MaxWidth="380"
                               HorizontalAlignment="Left"
                               Visibility="Collapsed" />
                </StackPanel>

                <!-- Right: Trailing content -->
                <ContentPresenter Grid.Column="1"
                                  VerticalAlignment="Center"
                                  Margin="14,0,0,0" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="RootBorder" Property="Background" Value="#08FFFFFF" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type ui:SettingsRowControl}">
        <Setter Property="Template" Value="{StaticResource SettingsRowControlTemplate}" />
        <Setter Property="Focusable" Value="False" />
    </Style>

</ResourceDictionary>
